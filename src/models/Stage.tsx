export default class {

    name: string;
    title: string;
    stageNumber: number;

    constructor(name:string, title:string, stageNumber:number) {
        this.name = name;
        this.title = title;
        this.stageNumber = stageNumber;
    }
}